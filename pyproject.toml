[project]
name = "python-for-devops"
version = "0.1.0"
description = "python-for-devops challenges"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.0",
]

[dependency-groups]
dev = [
    "fastapi[standard]>=0.136.0",
]
